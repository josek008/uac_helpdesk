<a href="#">
	<strong id="assigned" class="stat">
		<%= @user.tickets.count %>
	</strong>
	creados
</a>

<a href="#">
	<strong id="pending" class="stat">
		<%= @user.tickets.merge(Ticket.pending).count %>
	</strong>
	pendientes
</a>

<a href="#">
	<strong id="completed" class="stat">
		<%= @user.tickets.find_all_by_state("Cerrado").count %>
	</strong>
	completados
</a>